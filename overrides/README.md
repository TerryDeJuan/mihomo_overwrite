# Published overrides

This directory contains importable YAML and JavaScript override entry points. Keep these files stable and user-facing; experimental work belongs under `scripts/` or `tests/` until validated.
